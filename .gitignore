# 원본 스냅샷은 크고 스크립트로 다시 받을 수 있어 담지 않는다.
# (build/fetch_neis.py, build/fetch_population.py 참고)
data/raw/aca_*.csv
data/raw/school_*.csv

# 파이썬
__pycache__/
*.pyc
.venv/

# 작업용 임시 폴더
.tmp/

# 키는 절대 담지 않는다
data/secrets/

# 조사 문서 원본. 개인 표현이 있어 저장소에 올리지 않는다. 뽑은 결과만 web/data 에 둔다.
data/raw/*.docx

# 학교알리미 API 원본 (키로 다시 받는다)
data/raw/schoolinfo_*.json
